# -*- coding: utf-8 -*-
import os
import json

if __name__ == '__main__':


    '''
    json.loads() : 将json类型的str，转成python的数据结构(字典)
    json.dumps() : 将python的数据结构，存成json格式的str
    json.load() :  将json类型的file文件，转成python的数据结构(字典)
    json.dump() ： 将python的数据结构，存成json格式的str，并存入file中
    
    '''
