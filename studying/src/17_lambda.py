# -*- coding: utf-8 -*-
import os
import sys


def test_lambada_use():
    '''
    lambda 只包含一个语句，用于创建匿名函数。

    语法：

    lambda [arg1 [,arg2,.....argn]]:expression
    arg1 -- 参数，可以有多个
    expression -- 表达式

    f1 = lambda x : x > 10
    print(f1(1)) #输出：False

    '''


if __name__ == '__main__':

    test_lambada_use()
